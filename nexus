local Libary = loadstring(game:HttpGet("https://raw.githubusercontent.com/Frost230/Embee/refs/heads/main/Redz"))()
workspace.FallenPartsDestroyHeight = -math.huge

local Window = Libary:MakeWindow({
    Title = "Yellow Hub | Brookhaven RP 🏡",
    SubTitle = "by: Quetzy and Kasis999, Kayrus999",
    LoadText = "Carregando Yellow Hub",
    Flags = "Yellow Hub "
})
Window:AddMinimizeButton({
    Button = { Image = "rbxassetid://117731945306", BackgroundTransparency = 0 },
    Corner = { CornerRadius = UDim.new(35, 1) },
})

local InfoTab = Window:MakeTab({ Title = "Info", Icon = "rbxassetid://10723415903" })

InfoTab:AddDiscordInvite({
    Name = "Yellow Hub Studio",
    Description = "Join server",
    Logo = "rbxassetid://117731945306",
    Invite = "https://discord.gg/MbdySmsDVg",
}) 
InfoTab:AddSection({ "Informações do Script" })
InfoTab:AddParagraph({"Proprietário / Desenvolvedor:"})
InfoTab:AddParagraph({"Kasis999, ICEMANE, Quetzy"})
InfoTab:AddParagraph({ "Collaborations:", "Lyra Team" }) 
InfoTab:AddParagraph({ "Obsfucador:", "Quetzy" })
InfoTab:AddParagraph({ "Segurança:", "Quetzy" })
InfoTab:AddParagraph({ "Você está usando:", "Yellow Hub Brookhaven " })

InfoTab:AddSection({ "REJOIN" })
InfoTab:AddButton({
    Name = "REJOIN",
    Callback = function()
        local TeleportService = game:GetService("TeleportService")
        TeleportService:TeleportToPlaceInstance(game.PlaceId, game.JobId, game.Players.LocalPlayer)
    end
})

local TrollTab = Window:MakeTab({ "Trolling Map", "map" })



TrollTab:AddSection({ "Trolling Map" })

--[[

TrollTab:AddParagraph({ "Aviso", "ESTA FUNÇAO NAO E VISUAL" })

--]]

TrollTab:AddSection({ "SkyBox [ by Kasis999 Hub]" })

