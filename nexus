local Library = loadstring(game:HttpGet("https://raw.githubusercontent.com/silvaahub/anonymus-hub-/refs/heads/main/Library%20Amarela.txt"))()
workspace.FallenPartsDestroyHeight = -math.huge

local Window = Library:MakeWindow({
    Title = " Tiololo Hub | Brookhaven",
    SubTitle = "By: Ody",
    LoadText = "Carregando Tiololo Hub...",
    Flags = "Tiololohub_Brookhaven",
    Size = UDim2.fromScale(0.55, 0.65) -- Responsivo Mobile/PC
})

-- ðŸ”¹ BotÃ£o minimizar (Mobile Safe)
Window:AddMinimizeButton({
    Button = {
        Image = "rbxassetid://99919616573238",
        BackgroundTransparency = 1
    },
    Corner = { CornerRadius = UDim.new(1, 0) }
})

